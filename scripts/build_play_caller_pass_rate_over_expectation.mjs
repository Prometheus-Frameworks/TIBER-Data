#!/usr/bin/env node

console.log(`Play-caller pass rate over expectation lane scaffold only.

This first PR intentionally does not generate artifacts, fetch nflfastR/nflverse data, transcribe screenshot/reference values, or fabricate play-caller mappings.

Research notes and scope boundaries are documented at:
  docs/data/play-caller-pass-rate-over-expectation.md

The lane remains research/scaffold-only until source/provenance for play-caller mappings and expected-pass fields is settled.`);
