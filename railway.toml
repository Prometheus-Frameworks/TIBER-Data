[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/run_pipeline.py && uvicorn src.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
